export const metadata = {
  title: {
    default: "Admin - Daffodil Flower Shop",
    template: "%s - Daffodil Flower Shop",
  },
};

export default function AdminLayout({ children }) {
  return children;
}
